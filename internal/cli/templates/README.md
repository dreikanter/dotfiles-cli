# Dotfiles

Personal dotfiles managed with [dotfiles-cli](https://github.com/dreikanter/dotfiles-cli).

Install the tool and run `dotfiles --help` for usage:

```sh
go install github.com/dreikanter/dotfiles-cli/cmd/dotfiles@latest
dotfiles --help
```
